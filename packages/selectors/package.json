{
  "name": "@crawlio/selectors",
  "version": "0.1.0",
  "description": "Crawlio selector kernel — verification-gated selector primitives: robust XPath synthesis, array-xpath generalization, an exact-set verification oracle, and DOM inspection-view compaction. Shared by the browser agent and the headless engine. Ported from Intuned Selector Forge (MIT) + Chromium DevTools (BSD).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "typescript": "^5.6.2",
    "vitest": "^4.0.18"
  }
}
